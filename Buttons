#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    long long res = 0;

    for (int i = 0; i < n; i++) {
        res += (n - i) + (n - i - 1) * i;
    }

    cout << res << endl;
    return 0;
}
