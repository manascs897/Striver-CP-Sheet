#include <bits/stdc++.h>
using namespace std;

int main(){
	int a, b;
	cin >> a >> b;

	int count = 0;

	for (int i = 1; a <= b; i++) {
		count++;
		a *= 3;
		b *= 2;
	}

	cout << count;

    return 0;
}
